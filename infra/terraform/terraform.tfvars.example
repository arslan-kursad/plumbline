# Copy to terraform.tfvars (gitignored) and fill in.
project_id         = "PROJECT_ID"
billing_account_id = "012345-6789AB-CDEF01"

# Destination for the dead-letter depth alert. Never committed: this repository is
# public, and an address in it would be world-readable and not erasable.
alert_email        = "you@example.com"
