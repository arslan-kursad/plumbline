# collector

Go service that receives OTLP traffic and publishes raw payloads downstream.

Stub only (F0). Implementation is specified in F1. The collector never parses
span semantics — see `CLAUDE.md` boundaries.
