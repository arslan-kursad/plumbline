# normalization mappings v1.41

Placeholder. Mapping YAML for semantic-convention version 1.41 lands in F1.

Mappings live only in this tree — never in Firestore, never in environment
config. They are versioned in-repo and embedded at build time (ADR-0003).
